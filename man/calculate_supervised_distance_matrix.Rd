% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sorted_mat.R
\name{calculate_supervised_distance_matrix}
\alias{calculate_supervised_distance_matrix}
\title{Compute Supervised Distance Matrix}
\usage{
calculate_supervised_distance_matrix(X, Y, linkage)
}
\arguments{
\item{X}{Numeric matrix of observations.}

\item{Y}{Vector of category labels.}

\item{linkage}{Character linkage method.}
}
\value{
Symmetric distance matrix.
}
\description{
Builds a full pairwise distance matrix using supervised distance metric.
}
\examples{

X <- matrix(c(
 2, 0,   # pt1 (A)
 5, 1,   # pt2 (A)
 0, 4,   # pt3 (B)
 1, 5    # pt4 (B)
), byrow = TRUE, ncol = 2)
rownames(X) <- paste0("pt", 1:4)
Y <- factor(c("A", "A", "B", "B"))
# Case 1: Centroid linkage
calculate_supervised_distance_matrix(X, Y, linkage = "CT")
# Case 2: Single linkage
calculate_supervised_distance_matrix(X, Y, linkage = "SG")
# Case 3: Complete linkage
calculate_supervised_distance_matrix(X, Y, linkage = "CP")
}
